import { InsertHashtagPipe } from './insert-hashtag.pipe';

describe('InsertHashtagPipe', () => {
  it('create an instance', () => {
    const pipe = new InsertHashtagPipe();
    expect(pipe).toBeTruthy();
  });
});
