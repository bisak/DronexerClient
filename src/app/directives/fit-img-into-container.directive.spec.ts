import { FitImgIntoContainerDirective } from './fit-img-into-container.directive';

describe('FitImgIntoContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new FitImgIntoContainerDirective();
    expect(directive).toBeTruthy();
  });
});
